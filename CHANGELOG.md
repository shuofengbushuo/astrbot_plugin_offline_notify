> 本文件由 README.md 的「更新日志」章节同步生成，供 AstrBot WebUI 插件详情页展示。修改记录时请与 README.md 保持一致。

# 更新日志

### v2.0.2（2026-09-15）

**文档校正：QQ 官方适配器的会话缓存与离窗口发送**

- 📝 **纠正「缺少被动 msg_id 适配器静默丢弃」**：读 `qqofficial_platform_adapter.py` 源码确认——客户端适配器**不校验 5 分钟时效**，而是永久缓存 `_session_last_message_id` / `_session_scene`（无 TTL），且每次发送后把已发 id 写回缓存；`_allow_group_proactive_send` 默认 `True`，群在 `scene=="group"` 缓存下可**不带 msg_id** 主动推送。因此「5 分钟被动窗口」是 QQ **服务端**规则，适配器层离窗口发送是可行的。
- 📝 **说明各路径差异**：WebUI 测试 / 手动发送常能离窗口成功（借缓存或主动推送）；每日流程是插件刻意做成事件驱动（`_precheck` 默认 `allow_proactive=False` 拦截无 msg_id 主动推送），并非适配器做不到离窗口发。
- 📝 **澄清群 ID 必需性**：每日流程从 `config.target_groups` 取目标、为空不发送（无默认群）；两条路径都需要具体群 ID，区别只在目标来源与是否依赖实时消息（都不依赖）。
- ⚠️ 本版本为纯文档更新，无代码 / 行为变更。

### v2.0.1（2026-09-10）

**关窗不再降级发送 + 修复静默丢弃被误报成功**

- 🐛 **修复「发不出去却记成成功」**：QQ 官方适配器在缺少被动 `msg_id` 且群主动推送不成立时，会 `skip send_by_session` **直接 return**——既不抛异常也不返回 `False`。`notifier._send_single` 只判 `ret is False`，因此这类情况全被记为「成功发送」，还会 `mark_sent()` 导致当天不再重试，并在发布记录里留下假记录。现在 `_precheck` 复刻适配器判定（`has_msg_id` 或 `scene=="group" and _allow_group_proactive_send`），不具备发送条件一律提前判失败并给出真实原因。
- 🗑️ **移除关窗降级发送**（行为变更）：`_on_window_close` 不再调用 `_fire_plan(allow_proactive=True)`，只做 `window_state.disarm()` 并打 WARN 日志。原因：无被动 `msg_id` 时适配器必然静默丢弃，降级主动推送实际一条都发不出去。窗口内没机会 ⇒ 本次不发送。
- 🔧 **主动推送也要过检查**：`allow_proactive=True` 不再跳过前置检查，改为校验适配器是否真的具备群主动推送条件，避免手动「测试发送」在冷群上报假成功。
- 📝 **文案修正**：WebUI 定时任务徽章与栏目说明、插件描述同步更正——关窗的作用是「清理窗口状态」，不是「兜底发送」。
- ✅ **测试**：`test_qq_official_compat.py` 补全 `_allow_group_proactive_send` 模拟（此前缺失导致测不到放行分支），新增 4 条断言（scene=group 放行 / 冷群拦截 / 拦截原因 / 开关关闭拦截），全量通过。

### v2.0.0（2026-09-06）

**被动窗口事件驱动重构（破坏性变更）**

- 🚀 **触发机制重构**：放弃「到点主动推送 + 提前/浮动」，改为「时间窗口 + 事件驱动」。划定 `[下线时刻 − 窗口提前开启, 下线时刻 + 窗口延后关闭]`，窗口内检测到目标群发言（产生新鲜 msg_id）即触发发送，借被动消息窗口、不占主动配额；窗口内始终无人发言时，关窗降级为群内主动推送（不支持私聊兜底）。（注：关窗降级在 v2.0.1 已移除——实测 QQ 官方缺少被动 msg_id 时适配器静默丢弃，降级主动推送实际发不出，详见下方 v2.0.1 更新日志）
- 🆕 **新增配置项**：`window_before_minutes`（窗口提前开启，默认 30）、`window_after_minutes`（窗口延后关闭，默认 30）、`trigger_float_minutes`（触发时机浮动，默认 5，上限受被动 msg_id 5 分钟有效期约束）。
- 🗑️ **移除旧配置项**：`global_advance_minutes`、`global_float_range`、`qq_official_config`（`merge_segments` / `force_plain_text`）全部删除。
- 🗑️ **移除旧命令**：`/下线通知 测试` 已删除；`/下线通知 计划 添加` 不再接受 `[浮动]` 参数；`修改` 仅支持时间/日期。
- 🚫 **放弃 aiocqhttp**：仅支持 QQ 官方机器人（`qq_official` / `qq_official_webhook`），移除 OneBot/NapCat/go-cqhttp 适配与数字群号校验。
- 🆕 **每日去重**：`core/window_state.py` 维护窗口状态与每日发送去重，跨天自动重置、持久化到磁盘，重载/重启不重复发送。
- 🔧 **调度器重构**：`core/scheduler.py` 改为开窗/关窗 cron 双任务模型，跨天安全。
- ⚠️ **不向后兼容**：从 v1.x 升级需手动更新配置（删除旧键、新增窗口键、`target_groups` 改用 openid），详见 [常见问题](#如何升级到-v200)。

### v1.8.0（2026-08-16）

**配置项重构（提示词合并 + 重试重塑 + 注释修正）**

- 🔧 **提示词合并为单条**：删除「自动 / 手动」两套独立「砂糖」角色提示词（`BUILTIN_PROMPT` / `MANUAL_PROMPT`），改为单一完全自定义项 `custom_prompt`（默认值为原「砂糖」提示词文本），自动与手动通知共用同一条。
- 🔧 **重试配置重塑**：原易混淆的「最大重试次数」与「LLM 调用重试次数」拆分为两组正交配置，统一写入 `retry_config`——`send_retries` / `send_retry_interval`（管「发消息」）与 `llm_retries` / `llm_retry_interval`（管「生成内容」）；旧键名保留向后兼容。
- 📝 **注释重写**：修正 `merge_segments` 等配置项的错误注释（如「长通知合并为单条 auto」），统一为简洁正确描述；全文去除 `BUILTIN_PROMPT` / `MANUAL_PROMPT` / `custom_builtin_prompt` / `custom_manual_prompt` 等已废弃引用。
- ✅ **测试**：既有 `test_splitter_compat.py` / `test_qq_official_compat.py` 全部通过。

### v1.7.0（2026-08-08）

**QQ 官方机器人协议（qq_official）原生适配**

- 🆕 **平台能力适配层** `core/platform_compat.py`：运行时识别适配器类型，输出 `PlatformCaps` 能力画像（UMO 形态、分段策略、纯文本策略、段间隔），其余模块只依赖它，实现「一套代码多协议」。
- 🐛 **修复静默失败误报成功**（最严重）：`Context.send_message` 找不到平台实例时**只返回 `False` 不抛异常**，旧代码只捕获异常，因此会打出「成功发送通知到群 XXX」的假日志。现在显式检查返回值，并标记为不可重试（重试也没用），失败原因写入统计。
- 🐛 **修复 `platform_id` 错配**：从 aiocqhttp 切到 QQ 官方后，插件内置默认值 `小砂糖` 与实际实例名不符导致全部通知静默丢失。现在解析失败时自动回退到唯一已启用平台并给出修正建议；`platform_id` 也补进了配置面板。
- 🐛 **修复 openid 被误拒**：手动发送 / WebUI 测试接口原用 `isdigit()` 校验群号，会拒绝合法的 32 位十六进制 `group_openid`。改为按平台形态校验（`validate_target_id`），平台类型未知时放行不误伤第三方适配器。
- 🆕 **被动窗口探测与预警**：QQ 官方群消息需 5 分钟内的被动 `msg_id`，缺失时适配器**静默丢弃**。插件现在发送前主动探测，缺失时提前失败并说明真实原因；同时跟踪各会话最近入站时间，在调度触发前预警窗口是否已关闭。
- ⚡ **分段策略按平台切换**：QQ 官方下长通知**合并为单条**发送（换行保留分句节奏），避免逐段发送成倍消耗官方主动消息配额并触发频控；aiocqhttp 保持逐段发送不变。段间隔 0.8s → 官方 1.5s。
- ⚡ **强制纯文本**：QQ 官方适配器默认按 Markdown（`msg_type=2`）发送，需要事先报备模板。通知内容是纯散文，现强制 `use_markdown(False)`。
- ⚡ **告警通道加固**：告警目标同样做形态校验与真实成败判定；文档明确建议 QQ 官方下把 `alert_qq` 配成管理员 `user_openid`（C2C 主动推送不受 msg_id 限制）。
- 🆕 **配置节** `qq_official_config`：`merge_segments` / `force_plain_text` 开关，可覆盖上述自动策略。
- 🆕 **诊断工具** `tests/diagnose_live_config.py`：不启动 AstrBot 即可体检平台解析、目标标识、被动窗口，发现阻塞问题时退出码为 1。
- 🆕 **状态命令增强**：`/下线通知 状态` 新增发送平台行与 QQ 官方被动窗口诊断块。
- ✅ **测试**：新增 `tests/test_qq_official_compat.py`（8 组 53 项断言，覆盖平台解析/回退、标识校验、静默失败、分段合并、纯文本、告警通道、旧式调用向后兼容），全部通过；既有 `tests/test_splitter_compat.py` 回归全部通过。

### v1.6.0（2026-07-30）

**定时计划管理全面简化 + 多项修复**

- 🆕 **计划命令管理**：新增 `/下线通知 计划` 命令组（列表 / 添加 / 删除 / 启用 / 禁用 / 修改），用户无需编辑 JSON 即可管理定时计划；底层 `schedules.json` 持久化存储（`core/schedule_store.py`），首次启动自动从旧配置导入。
- 🆕 **WebUI 计划管理面板**：仪表盘新增「定时计划管理」卡片，可视化增删改与启用/禁用，与命令共享同一数据源。
- 🔧 **提前通知时间全局化**：移除计划级 `advance_minutes` 字段，改由配置面板「提前通知时间」统一控制所有计划；**设为 0 则准时发送**。调度器 `_add_job` 直接使用全局值。
- 🔧 **调度器热重载**：新增 `reload_jobs()`，计划变更后无需重启插件即生效。
- 🐛 **冒号空格容错**：修复命令中 `时间 10: 30` 因空格截断导致字段解析失败、消息穿透到 LLM 的问题；现兼容中英文冒号（`：`/`:`）及冒号两侧空格。
- 🐛 **目标群组兼容**：`notifier.py` 的 `send_to_groups` 兼容 `target_groups` 为纯字符串列表（`["123"]`）或字典列表（`[{"group_id": ...}]`）两种格式，修复 `'str' object has no attribute 'get'`。
- 📝 **命令描述精简**：`/下线通知 计划`、`/下线通知 提示词` 的面板描述从 20+ 行精简为 4 行，详细用法在聊天中输入命令展开。
- 📝 **配置 schema 兼容**：`schedules` 配置项恢复为 `list` 类型（避免旧配置升级时类型校验失败），仅作向后兼容占位。

### v1.5.0

- 默认提示词自称改为「砂糖」（群友也叫"小砂糖"），恢复「真实的小姑娘」身份声明；
- 提示词「明置」——新增配置项 `custom_builtin_prompt` / `custom_manual_prompt` 可在面板直接编辑；
- 新增命名方案库 `/下线通知 提示词` 命令，支持多套提示词独立保存 / 切换 / 删除（三级优先级：激活方案 > 配置自定义 > 内置默认）。

### v1.4.4

- 内置提示词改为 16 岁真人女孩「砂糖」人设自然道别（v1.4.1 起自带按句分段，不依赖 splitter 插件）。

### v1.4.1

- 长通知自动按句分段发送，不再依赖 `astrbot_plugin_splitter` 插件。

### v1.3.0

- 管理员私聊手动生成并发送到指定群（`/下线通知 生成 [群号]`）；
- 调度器心跳检测机制。

### v1.2.0

- 浮动时间机制（实际触发时间在 advance ~ advance+float_range 间随机浮动）；
- 通知发布记录持久化存储与查询。

### v1.1.0

- LLM 通知内容生成（调用对话模型生成多样化、自然的下线通知）。
